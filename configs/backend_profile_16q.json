{
  "seed": 20260916,
  "parents": 6,
  "families": [
    "spin_glass",
    "weighted_maxcut"
  ],
  "logical_qubits": 8,
  "chain_lengths": [
    2,
    2,
    2,
    2,
    2,
    2,
    2,
    2
  ],
  "variants": [
    {
      "shape": "path",
      "ports": 1,
      "field_distribution": "uniform"
    },
    {
      "shape": "random_tree",
      "ports": 2,
      "field_distribution": "concentrated",
      "coupling_distribution": "random"
    }
  ],
  "chain_strengths": [
    0.75,
    1.5,
    3.0
  ],
  "runtimes": [
    4.0
  ],
  "candidates": 8,
  "candidate_batch_size": 4,
  "spectral_points": 3,
  "teacher": {
    "mode": "none"
  },
  "steps": 64,
  "max_steps": 16384,
  "label_state_tolerance": 0.0002,
  "max_physical_qubits": 16,
  "endpoint_memory_mb": 2050,
  "state_memory_mb": 4104,
  "endpoint_max_qubits": 16,
  "_note": "Backend throughput at 16 physical qubits. The point of this ladder is that only the spectral teacher is exponentially capped: a full eigendecomposition needs 6.9e+01 GB at this size, while the state vector needs 0.001 GB and the basis observables 0.003 GB. teacher.mode=none, so this measures the propagation path that every non-oracle claim in the project actually uses."
}
