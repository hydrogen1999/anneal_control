{
  "seed": 20260916,
  "parents": 6,
  "families": [
    "spin_glass",
    "weighted_maxcut"
  ],
  "logical_qubits": 6,
  "chain_lengths": [
    2,
    2,
    2,
    2,
    2,
    2
  ],
  "variants": [
    {
      "shape": "path",
      "ports": 1,
      "field_distribution": "uniform"
    },
    {
      "shape": "random_tree",
      "ports": 2,
      "field_distribution": "concentrated",
      "coupling_distribution": "random"
    }
  ],
  "chain_strengths": [
    0.75,
    1.5,
    3.0
  ],
  "runtimes": [
    4.0
  ],
  "candidates": 32,
  "candidate_batch_size": 8,
  "spectral_points": 3,
  "teacher": {
    "mode": "none"
  },
  "steps": 64,
  "max_steps": 16384,
  "label_state_tolerance": 0.0002,
  "max_physical_qubits": 12,
  "endpoint_memory_mb": 1024,
  "state_memory_mb": 4096,
  "endpoint_max_qubits": 12,
  "_note": "Backend crossover at 12 physical qubits, 6 parents x 32 candidates = 192 labels over 2 families. Parents must be at least three per family (6 here) or generation.parent_splits cannot fill train/validation/test. teacher.mode=none, so this measures propagation throughput. Fewer labels at large sizes amortise CuPy's fixed startup over less work, biasing the ratio DOWN against the GPU -- the conservative direction."
}
