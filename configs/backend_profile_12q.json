{
  "seed": 20260916,
  "parents": 8,
  "families": [
    "spin_glass",
    "weighted_maxcut"
  ],
  "logical_qubits": 6,
  "chain_lengths": [
    2,
    2,
    2,
    2,
    2,
    2
  ],
  "variants": [
    {
      "shape": "path",
      "ports": 1,
      "field_distribution": "uniform"
    },
    {
      "shape": "random_tree",
      "ports": 2,
      "field_distribution": "concentrated",
      "coupling_distribution": "random"
    }
  ],
  "chain_strengths": [
    0.75,
    1.5,
    3.0
  ],
  "runtimes": [
    4.0
  ],
  "candidates": 32,
  "candidate_batch_size": 16,
  "spectral_points": 3,
  "teacher": {
    "mode": "none"
  },
  "steps": 64,
  "max_steps": 16384,
  "label_state_tolerance": 0.0002,
  "max_physical_qubits": 12,
  "endpoint_memory_mb": 1024,
  "state_memory_mb": 4096,
  "_note": "Backend crossover at 12 physical qubits (6 logical x chain length 2), 8 parents x 32 candidates = 256 labels. teacher.mode=none, so this measures propagation throughput, not teacher throughput. Label counts shrink with size deliberately: NumPy cost grows exponentially and a fixed workload would take hours at 16 qubits. Throughput is per label, so the sizes stay comparable -- but with few labels CuPy's fixed startup is amortised over less work, which biases the measured ratio DOWN, against the GPU. That is the conservative direction and is preferred to the opposite."
}
