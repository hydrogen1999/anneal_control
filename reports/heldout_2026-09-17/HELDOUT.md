> Updated inference: use [the 2026-09-18 reanalysis](../evidence_audit_2026-09-18/RESULTS.md). Historical contrast p-values below used the previous test; they are retained for provenance, not current hypothesis decisions. The pooled information contrast is outside the pairwise Holm family.

# Held-out comparison: does the architecture beat the simpler encoders?

Source: `research_v1`, 5 encoders × 5 training seeds = 25 runs, all trained and
evaluated. 48 test parents, 864 test records, split by parent. Lower loss is
better; loss = 1 − decoded logical success. Every interval below is a parent
bootstrap, the parent being the unit of independence.

Reproduce with:

```
annealctrl method-contrast --records reports/heldout_2026-09-17/heldout_records.json \
    --output reports/heldout_2026-09-17/method_contrasts.json --bootstrap-resamples 20000
```

## 1. Per-method table

See `RESULTS.md` (generated by `annealctrl report`). Each row is that method's
difference from the **global** baseline, not from the other methods.

## 2. Method against method, Holm-corrected

Ten comparisons per mode; a pair is called separated only after Holm correction
over the whole family. Two intervals that both exclude zero are not a test of
their difference, which is why this table exists.

### Bank selection

| comparison | difference | 95% CI | Holm p | separated |
|---|---:|---|---:|:--:|
| hierarchy_outcome vs logical | +0.00761 | [+0.00461, +0.01056] | 0.0010 | **yes** |
| hierarchy_physics vs logical | +0.00816 | [+0.00475, +0.01156] | 0.0010 | **yes** |
| logical vs summary | −0.00852 | [−0.01205, −0.00497] | 0.0016 | **yes** |
| logical vs physical | −0.00699 | [−0.01100, −0.00293] | 0.0077 | **yes** |
| hierarchy_physics vs physical | +0.00117 | [−0.00041, +0.00285] | 0.9120 | no |
| hierarchy_outcome vs hierarchy_physics | −0.00055 | [−0.00166, +0.00050] | 1.0000 | no |
| hierarchy_outcome vs physical | +0.00062 | [−0.00114, +0.00253] | 1.0000 | no |
| hierarchy_outcome vs summary | −0.00091 | [−0.00262, +0.00080] | 1.0000 | no |
| hierarchy_physics vs summary | −0.00036 | [−0.00236, +0.00167] | 1.0000 | no |
| physical vs summary | −0.00153 | [−0.00411, +0.00082] | 1.0000 | no |

Indistinguishable group: `{hierarchy_outcome, hierarchy_physics, physical,
summary}`. Separated on its own: `{logical}`.

**Every separation in this table is the embedding-blind encoder losing to an
embedding-aware one. No embedding-aware encoder separates from any other.**

### Direct proposal

Indistinguishable group: `{hierarchy_outcome, hierarchy_physics, logical,
summary}`; `physical` separates as the *worst*. In direct mode the
embedding-blind encoder is not distinguishable from the aware ones at all.

## 3. The information question

Embedding-aware encoders pooled against the embedding-blind one, bank mode:

| quantity | value |
|---|---|
| aware | `hierarchy_outcome`, `hierarchy_physics`, `physical`, `summary` |
| mean aware | 0.54536 |
| mean blind (`logical`) | 0.55318 |
| difference | **−0.00782**, 95% CI [−0.01109, −0.00447], p = 1e-4 |
| parents favouring aware | 36/48 |

In direct mode the same contrast is +0.00259 [−0.00507, +0.00995] — **not**
separated.

## 4. What this supports, and what it does not

**Supported.** An encoder that can see the embedding beats one that cannot, when
selecting from a bank: −0.0078 [−0.0111, −0.0045], parent-paired, exploratory and unadjusted
against the full family of comparisons. This is the learned-model counterpart of
the G3 intervention result, measured on held-out parents, and it is the claim
the paper can make.

**Not supported.** That the hierarchical architecture is better than a pooled
summary encoder. `summary` is the cheapest embedding-aware encoder in the set and
it is first in the ranking; the four aware encoders are mutually
not separated at this sample size (not an equivalence finding). The honest reading is that the *encoder
architecture* does not matter once the embedding is visible at all — the
information does the work, not the elaboration.

**Not supported.** Any claim about direct generation. All five encoders land
within noise of each other at ≈0.590 against a 0.601 linear baseline, and bank
selection reaches ≈0.545. Generation, not representation, is where the method
loses.

**Power, not equivalence.** Failing to separate is not proof of equality. With 48
test parents the narrowest aware-vs-aware interval here is ±0.0011; a real effect
smaller than that would not have been detected, and no equivalence test was run.
